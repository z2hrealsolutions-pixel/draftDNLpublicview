# Same Supabase project as the other two DNL apps - fill in your values.
# Find both under Supabase Dashboard -> Project Settings -> API.
# This app never reads or writes anything sensitive - every function it
# calls is deliberately built to exclude the referee OTP entirely.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key-here
